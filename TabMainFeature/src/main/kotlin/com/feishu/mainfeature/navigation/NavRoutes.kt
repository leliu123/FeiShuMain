package com.feishu.mainfeature.navigation

/**
 * Centralizes navigation route names used inside the TabMainFeature module so that
 * we do not rely on scattered hard-coded strings when navigating to shared flows.
 */
const val ROUTE_MAIN = "main_screen"
const val ROUTE_AI_CHAT = "ai_chat"
